{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.5.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "turbo": "^2.5.3"
  }
}
